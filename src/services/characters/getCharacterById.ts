/* import { createApi, fetchBaseQuery } from '@reduxjs/toolkit/query/react'

export const characterAPI = createApi({
    reducerPath: "selectedCharacter",
    baseQuery: fetchBaseQuery({baseUrl: ""}),
    endpoints: (builder) => {
        
    }
}) */
