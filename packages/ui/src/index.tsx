import  {Button} from "./components/button/button"
import React  from "react"

interface Props {
    name: string
}

export const InnerButton = () => {
    return <Button text="sdasd" onClick={()=>{}} />
}